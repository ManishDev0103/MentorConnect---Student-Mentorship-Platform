Summary of changes made (2026-07-29)

Files modified/added and brief descriptions:

- client/src/service/userService.js
  - Added `deleteAccount()` API helper; existing `updateEmailPreferences()` present.

- client/src/Component/Profile/AccountSettings.jsx
  - Imported and used `deleteAccount()`; fixed delete account flow to call service helper and handle errors.

- client/src/service/authApiService.js
  - Added `validateResetToken()` API call (GET /api/users/validate-reset-token).

- client/src/pages/ResetPassword/ResetPasswordPage.jsx
  - UI: token validation, password strength meter, client-side strict password policy checks and improved UX for reset flow.

- server/MentorshipBackend/src/main/java/com/mentorship/validation/ValidPassword.java
  - New annotation for password validation (enforces policy on DTOs).

- server/MentorshipBackend/src/main/java/com/mentorship/validation/PasswordConstraintValidator.java
  - New validator implementing the password policy (upper/lower/number/special and length 8-20).

- server/MentorshipBackend/src/main/java/com/mentorship/dtos/ResetPasswordRequest.java
  - Added `@ValidPassword` on `newPassword`; DTO corrected/validated.

- server/MentorshipBackend/src/main/java/com/mentorship/dtos/MentorSignupRequest.java
  - Added `ratePerSession` and `discountPercent` fields to match service usage.

- server/MentorshipBackend/src/main/java/com/mentorship/dtos/StudentSignupRequest.java
  - Fixed class closing brace; ensured DTO compiles.

- server/MentorshipBackend/src/main/java/com/mentorship/service/UserServiceImpl.java
  - Fixed image upload null checks and content-type handling to avoid NPE; removed unused imports; implemented reset-token validation, reset flow, email preference update, and other improvements.

- server/MentorshipBackend/src/main/java/com/mentorship/controller/UserController.java
  - Added endpoints: `GET /validate-reset-token`, `PATCH /reset-password`, `PATCH /email-preferences`, `DELETE /me`, and profile image endpoints; cleaned imports.

- server/MentorshipBackend/src/main/java/com/mentorship/dtos/EmailPreferenceRequest.java
  - New DTO for email preference updates.

- server/MentorshipBackend/src/main/java/com/mentorship/dtos/UserResp.java
  - Added `emailNotificationsEnabled` to the response DTO.

- server/MentorshipBackend/src/main/java/com/mentorship/exception/GlobalExceptionHandler.java
  - Handler already updated earlier to return structured messages; no new change today aside from review.

- server/MentorshipBackend/src/main/java/com/mentorship/config/DefaultAdminInitializer.java
  - Removed default hardcoded admin defaults; initializer now requires `default.admin.email` and `default.admin.password` to be provided (via env/properties) before creating an admin.

- server/MentorshipBackend/src/main/resources/application.properties
  - Removed hardcoded default admin credentials; added comment instructing use of environment variables instead.

Notes & security context:
- Removed hardcoded admin credentials from properties and made admin creation opt-in via env vars.
- Fixed multiple compilation/validation issues in DTOs and services that were preventing compile.
- Implemented safer handling for file uploads (NPE guard, size check), and better password validation support across DTOs.

How to create initial admin now:
- Provide the properties at startup (environment variables or Spring properties). Example (PowerShell):
  $env:DEFAULT_ADMIN_EMAIL='admin@example.com'
  $env:DEFAULT_ADMIN_PASSWORD='StrongP@ssw0rd!'
  ./mvnw spring-boot:run

If you want, I can also:
- Add an example `application-local.properties` (gitignored) with instructions.
- Create an admin management endpoint protected by a one-time secret if you prefer runtime admin creation rather than env vars.

End of summary.
